import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nextjs-starter-medusa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
